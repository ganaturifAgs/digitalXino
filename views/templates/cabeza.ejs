<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="favicon.png">
    <link rel="stylesheet" href="css/main.css">
    <link rel="stylesheet" href="css/fontawesome/all.css">
    <script src="js/jquery.min.js"></script>
    <script src="js/metodos.js"></script>
    <script src="js/fontawesome/all.js"></script>
    <script src="js/qrcode.min.js" ></script>
    <title><%= title || 'Mi Sitio Web' %></title>

</head>
<body>
    <header>
        <div><%= headerTitle || 'Bienvenido a Mi Sitio' %>
            <div id="der" class="rosa"><img src="img/derecha-arriba.png" width="100%"  alt=""></div>
            <div id="izq" class="rosa"><img src="img/derecha-arriba.png" width="100%"  alt=""></div>
            <div id="countdown" ></div>
            <div id="countTipos">D  H  M  S</div>
        </div>
        <script>
            let cero = (v)=>{ return  v<10 ? `0${v}`:v }
            function updateCountdown() {
                const targetDate = new Date('2025-08-16T14:00:00');
                const now = new Date();
                const diff = targetDate - now;
                if (diff <= 0) {
                    document.getElementById('countdown').textContent = '¡El evento ha comenzado!';
                    return;
                }
                const dias = Math.floor(diff/ (1000*60*60*24))
                const hours = Math.floor((diff % (1000*60*60*24)) / (1000 * 60 * 60));
                const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
                const seconds = Math.floor((diff % (1000 * 60)) / 1000);

                document.getElementById('countdown').textContent =
                    `Faltan: ${dias} . ${cero(hours)} : ${cero(minutes)} : ${cero(seconds)}`;
            }

            updateCountdown();
            setInterval(updateCountdown, 1000);
        </script>
    </header>
